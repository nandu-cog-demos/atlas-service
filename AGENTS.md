# Atlas Service — agent notes

- Backend: FastAPI in src/api. Run tests with `pytest`.
- Frontend: React/TS in web/. Run tests with `npm test`.
- Use Pydantic models for all request/response bodies.
- Use parameterized queries only; never interpolate SQL.
